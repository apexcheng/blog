---
title: MDX 内容展示
description: 代码块、Callout 和 Mermaid 图表的展示示例。
---

import Callout from '../../../components/Callout.astro';
import Mermaid from '../../../components/Mermaid.astro';

这页保留给 Starlight 文档区，后续可以放更系统的教程和图表示例。

<Callout title="内容原则">
图表用于降低理解成本，不为了装饰硬加。
</Callout>

```python
def load_cache(file_path):
    data = read_json(file_path)
    return data
```

<Mermaid
  title="内容发布架构"
  chart={`flowchart LR
    A[Markdown / MDX] --> B[Astro 内容集合]
    B --> C[页面模板]
    C --> D[静态站点]`}
/>

<Mermaid
  title="文档内容流程"
  chart={`flowchart LR
    A[Markdown] --> B[MDX]
    B --> C[静态站点]`}
/>

<Mermaid
  title="自动化校验时序"
  chart={`sequenceDiagram
    participant Author as 作者
    participant Build as 构建命令
    participant Page as 页面
    Author->>Build: npm run build
    Build->>Page: 生成静态 HTML
    Page-->>Author: 返回构建结果`}
/>

<Callout title="注意">
这页主要验证 Starlight 文档区也能复用博客里的提示块和图表组件。
</Callout>

<Callout title="检查">
发布前至少运行一次构建，确认 MDX、代码块和 Mermaid 都能正常渲染。
</Callout>
